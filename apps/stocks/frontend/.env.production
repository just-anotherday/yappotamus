# Production Environment (Cloudflare Pages)
# These values are used when building for production deployment.
# In Cloudflare Pages, configure these in: Pages → Settings → Environment Variables

NEXT_PUBLIC_API_BASE=https://yapvibes-stocks-api.onrender.com
