version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: yapvibes-website-backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/ai-generator-backend:/app
    working_dir: /app
