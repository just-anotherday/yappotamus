import { useState } from 'react'
import { Temporal } from '@js-temporal/polyfill'
import { useUserSettings } from '../../hooks/useUserSettings'
import { useEntityReminder } from '../../hooks/useEntityReminder'
import type { ReminderTarget } from '../../types/reminders'
export function ReminderControl({target}:{target:ReminderTarget}) { const {settings}=useUserSettings(); const state=useEntityReminder(target); const [open,setOpen]=useState(false); const zone=settings?.timezone; const current=state.reminder; const local=current&&zone?Temporal.Instant.from(current.remind_at).toZonedDateTimeISO(current.timezone):null; const [date,setDate]=useState(local?.toPlainDate().toString() ?? Temporal.Now.zonedDateTimeISO(zone ?? 'UTC').toPlainDate().toString()); const [time,setTime]=useState(local?.toPlainTime().toString({smallestUnit:'minute'}) ?? '09:00'); const [validation,setValidation]=useState<string|null>(null); const save=async()=>{if(!zone){setValidation('Your timezone is still being set up. Please try again shortly.');return} try { const plain=Temporal.PlainDateTime.from(`${date}T${time}`); const instant=Temporal.ZonedDateTime.from({year:plain.year,month:plain.month,day:plain.day,hour:plain.hour,minute:plain.minute,second:plain.second,timeZone:zone},{disambiguation:'reject'}).toInstant(); if(instant.epochMilliseconds < Date.now()-60000){setValidation('Choose a future time.');return} if(await state.save(date,time,zone,instant.toString())) setOpen(false) } catch {setValidation('This time is invalid or occurs twice because of daylight saving time. Choose another time.')} }; return <div className="mt-2 text-xs">{current ? <><p className="text-stone-500">Reminder {Temporal.Instant.from(current.remind_at).toZonedDateTimeISO(current.timezone).toLocaleString('en-US',{dateStyle:'medium',timeStyle:'short'})} · In app</p><button type="button" onClick={()=>setOpen(true)} className="mr-2 text-emerald-700">Edit Reminder</button><button type="button" disabled={state.pending} onClick={()=>void state.cancel()} className="text-red-700">Remove Reminder</button></> : <button type="button" onClick={()=>setOpen(true)} className="text-emerald-700">Set Reminder</button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><form onSubmit={e=>{e.preventDefault();void save()}} className="w-full max-w-sm rounded-xl bg-white p-5 dark:bg-stone-900"><h3 className="text-lg font-semibold">{current?'Edit Reminder':'Set Reminder'}</h3><label className="mt-4 block">Date<input type="date" value={date} onChange={e=>setDate(e.target.value)} className="mt-1 w-full border p-2" required/></label><label className="mt-3 block">Time<input type="time" value={time} onChange={e=>setTime(e.target.value)} className="mt-1 w-full border p-2" required/></label><p className="mt-3">✓ In app</p>{validation&&<p className="mt-2 text-red-700">{validation}</p>}{state.error&&<p className="mt-2 text-red-700">{state.error}</p>}<div className="mt-5 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)}>Cancel</button><button disabled={state.pending} className="rounded bg-emerald-700 px-3 py-2 text-white">Save Reminder</button></div></form></div>}</div> }
